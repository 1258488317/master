http://test-ats.fengdai.org
18505816257
ats123456


资产端，即资金需求方即融资方；资金端，即出借人一方，也就是投资挣利息收益的一方。

中农君拿银行来举个例子，对于向银行贷款的企业或个人来说，银行就是资金端，而与之相对应的借款者——企业或个人，就是资产端。

python：
1.注意我们使用过的术语 —— 函数中的参数名称为形参而你提 供给函数调用的值称为实参。 
2.global 语句被用来声明 x 是全局的 
3.封装：https://www.cnblogs.com/Michael--chen/p/6740455.html
4.注意，默认参数值应该是一个参数。更加准确的说，默认参数值应该是不可变的 
5.只有在形参表末尾的那些参数可以有默认参数值，即你不能在声明函数形参的时候，先声明有默 认值的形参而后声明没有默认值的形参。这是因为赋给形参的值是根据位置而赋值的。例如，def func(a, b=5) 是有效的，但是 def func (a=5, b) 是无效的。
6.VarArgs